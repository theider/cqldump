# cqldump
Cassandra import/export web tool.

Export dumps to the browser a ZIP file containing metadata and JSON records for all Cassandra data.

Import takes a ZIP file in the same format and exports to a new keyspace with a specified replication.

